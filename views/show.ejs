<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ("./partials/head.ejs") %>
    <title><%= csp.council %></title>
</head>
<body>
    <%- include ("./partials/header.ejs") %>
    <div class="container">
        <div class="showpage">
            <h2>State: <%= csp.state %></h2>
                <img class="cspshowimage" src="<%= csp.image %>" alt="<%= csp.council %>">
        
        <p><h3>Council: <%= csp.council %></h3></p>
        <p><h3>Issue: <%= csp.issue %></h3></p>

        <p>Council Location:</p>
        <iframe src="<%= csp.location %>" width="400" height="300" style="border:0;" allowfullscreen="" loading="lazy"></iframe>

        <!-- Delete button -->
        <form action="/cspcollection/<%= csp.id %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE">
        </form>

        <!-- Send user to Edit Page -->
        <button><a href="/cspcollection/<%= csp.id %>/edit">Edit</a></button>

    </div>
    </div>
    <%- include ("./partials/footer.ejs") %>
</body>
</html>